import { Box } from '@mui/material';
import React from 'react';

const CartPage = () => {
    return (
        <Box sx={{ m: 10, mt: 10 }}>
            
        </Box>
    );
};

export default CartPage;